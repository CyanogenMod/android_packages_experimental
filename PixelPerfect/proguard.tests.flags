# Proguard flags specifically needed for tests.

# TODO(stlafon): Remove the following when we start using ScreenshotGrabber in the src.
-keep class com.google.android.apps.pixelperfect.ScreenshotGrabber
-keepclassmember class com.google.android.apps.pixelperfect.ScreenshotGrabber {
  float getDegreesForRotation(int);
}

-keepclassmember class com.google.android.apps.pixelperfect.ExcludedPackages {
  static ExcludedPackages getInstanceForTests(android.content.Context, java.util.Set);
}
