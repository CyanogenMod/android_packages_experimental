# Proguard flags specifically needed for tests.

# TODO(stlafon): Remove those when we start using the ExcludedPackages methods.
-keepclassmember class com.google.android.apps.pixelperfect.ExcludedPackages {
  public void addCustom(java.lang.String);
  public boolean removeCustom(java.lang.String);
}
